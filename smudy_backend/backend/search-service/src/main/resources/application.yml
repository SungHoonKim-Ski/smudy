server:
  port: 7072

spring:
  application:
    name: search-service
  elasticsearch:
    uris: ENC(7T0qUSOj2oEz+37r40gXHw098A8orVSU4lhVoPpOszlAcF3qEQjDcw==)
    username: ENC(Lu01ZKBRI/LDxQV7TGL1tw==)
    password: ENC(lAOGk9gc8PTKBWHz4ZFNSVvKd1OiCF/h)

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8762/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 1
    lease-expiration-duration-in-seconds: 2