server:
  port: 8080

spring:
  application:
    name: backend_dummy
  servlet:
    multipart:
      enabled: true
      max-file-size: 100MB  # 필요에 따라 파일 크기 조절
      max-request-size: 100MB  # 필요에 따라 요청 크기 조절

logging:
  level:
    root: debug
